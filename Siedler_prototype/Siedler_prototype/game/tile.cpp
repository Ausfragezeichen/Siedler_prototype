
#include "tile.h"

//class Tile



//class SandTile
void SandTile::draw(sf::RenderWindow& window) const {
	window.draw(this->sprite);
}

//class GrasTile
void GrasTile::draw(sf::RenderWindow& window) const {
	window.draw(this->sprite);
}